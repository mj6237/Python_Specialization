# Take a float number as input and convert it to an integer. Print both versions. 

user_flot = float(input("Enter a float number :"))
print("Input type is :",type(user_flot))
converted_int = int(user_flot)
print ("Now its converted in :" ,type(converted_int))